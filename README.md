# Eason Cover Flow

V1 prototype for a private birthday music page. Click the focused center cover to play or pause its audio. Three real cover assets and matching M4A files are connected already.

```bash
npm install
npm run dev
```
